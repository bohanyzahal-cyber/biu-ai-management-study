# 🎯 דף עזר לבחינה — יישומי בינה מלאכותית בניהול (865591903)

> דף עזר דחוס לחיפוש מהיר במבחן הפתוח. מסודר לפי הרצאה + טבלאות "שליפה מהירה" בסוף.
> ⚡ הדגשים: **מונח = הגדרה**, סדרים/רשימות, ומספרים שנשאלים.

---

## הרצאה 01 · AI כווקטורים ולמידת מכונה
- **ייצוג וקטורי**: כל תכונה = ממד. תמונת 1000×1000 = וקטור ב-**מיליון** מימדים. פריטים דומים = **קרובים** במרחב; "למידה" = מציאת **הגבול המפריד** בין קבוצות.
- **למידת מכונה (ML)**: יכולת ללמוד מנתונים קיימים ולהסיק על נתונים לא-ידועים.
- **4 סוגי משימות ML** (⚠️ נשאל הרבה):
  - **רגרסיה** → חיזוי ערך מספרי **רציף** (מחיר מניה, כמות ביקוש)
  - **סיווג/קלסיפיקציה** → שיוך לקטגוריות **בדידות** (חתול/כלב, ספאם/לא)
  - **זיהוי חריגות (Anomaly)** → נקודות **נדירות** הרחוקות מהנורמלי (הונאה, חשבונית חשודה)
  - **למידת חיזוק (RL)** → סוכן לומד מ**תגמול** על פעולות רצופות (שחמט, רחפן)
- **Overfitting**: מודל שלמד "בעל פה" — מצליח רק בשאלות זהות.
- **Deep Learning**: תת-תחום ML מבוסס **רשת נוירונים**. **פיצ'ר (Feature)** = תכונה בודדת נמדדת.
- **סיכון אבטחה** בשליחה ל-LLM ענן: המידע "יוצא החוצה" (man-in-the-middle).

## הרצאה 03 · טרמינל ו-Codex
- **Codex** = ה-CLI של OpenAI. סוכן עובד בטרמינל כי הוא יודע רק **טקסט** (לא עכבר).
- **פקודות**: `mkdir` (תיקייה), `python file.py` (הרצה), `cd` (מעבר). Windows: מפריד נתיב = **בקסלאש `\`**.
- **סדר התקנה**: PowerShell → Node.js + npm → הרשאת הרצה → Codex → התחברות. אחרי Node.js **לסגור ולפתוח מחדש טרמינל** (טעינת סביבה).
- **npm** = מנהל חבילות (מגיע עם Node). התקנה: `npm install -g @openai/codex`.
- **`codex login`** → אימות **OAuth 2.0**. **`/exit`** יוצא מ-Codex ונשאר בטרמינל; **`exit`** סוגר טרמינל.
- **פרויקט = תיקייה** אחת המרכזת את כל קבצי המשימה.

## הרצאה 04 · כלכלת ה-AI
- **מחיר מוצרי מידע**: P → MC → **0** (עלות שולית אפסית / *Zero Marginal Cost Society*).
- **המטבע החדש** = **תשומת הלב (Attention)** / הקשב האנושי (כלכלת שפע).
- **Jagged Frontier** (הטכנולוגיה המשוננת): גבול יכולת ה-AI **לא אחיד** — הצלחות בגבול, נפילות מחוצה לו.
- **Noy & Zhang (Science)**: ChatGPT ↓זמן משימה **40%**, ↑איכות **18%**.
- **דחיסת פריון**: העובדים ה**חלשים** נהנים הכי הרבה → הפער נסגר.
- **פתרון רגולטורי (סגל)**: **מס על סוכני AI** למימון המעבר.
- **מגן הסיליקון (Silicon Shield)**: תלות ב-**TSMC** (90%+ שבבים) כביטוח לטייואן.
- **ההשטחה הגדולה**: טווח שליטה **8.1 (2013) → 12.1 (2025)**.
- **תלות גומלין נשקית**: שימוש בצווארי בקבוק של שרשראות ערך ככלי כפייה.
- **סוכן אוטונומי (Agentic AI)**: מבצע משימות מקצה-לקצה שדרשו מומחיות אנושית.
- **כשל סוציאליזם (ויכוח החישוב)**: כשל **אפיסטמי** — אין חישוב כלכלי רציונלי ללא שוק.

## הרצאה 05 · Vibe Coding
- **הגדרה**: כתיבת קוד בשפה **חופשית/טבעית**, בלי לדעת לתכנת. מתארים "מה", ה-LLM כותב "איך".
- **4 שלבים (סדר!)**: **הגדרת משימה → יצירת קוד → שמירה → הרצה**.
- פייתון = **המנוע** (במשל). ספריית השעון = **Turtle**. קובץ = סיומת **`.py`** בלי רווחים.
- כשה-AI טועה → **"כועסים עליו"** (מפנים ביקורת ומבקשים תיקון). Claude Code: כל 4 השלבים במקום אחד.

## הרצאה 06 · שרשרת אספקה (תאוריה)
- **שיחה vs סוכן**: שיחה = שאלה-תשובה; **סוכן** = מקבל מטרה, מפרק לתת-משימות, מבצע עם **כלים (Tools)**.
- אין לסמוך על **חישוב מתמטי** של LLM (יודע סטטיסטיקה, לא חשבון). **הזיה** = המצאת מידע "מה הכי הגיוני".
- **2 שלבי חיי מודל**: **אימון (Training)** ו-**הסקה (Inference)**. **תאריך תוקף** = היום האחרון שאומן עד אליו.
- **כלכלת AI חיובית** = תמיד **"אדם בלולאה"** מבקר ואחראי.
- **Claude** = "אלוף העולם בכתיבת קוד". **Nano Banana (Gemini)** = יצירת תמונות. **Ollama** = הרצה מקומית (אין עלות טוקנים, מידע בבית).
- **Prompt Engineering** = ניסוח בקשות מדויקות. כלל 4: לבקש **"חריגות" + להיות ביקורתי** בבדיקת מסמכים.

## הרצאה 07 · תרגול שרשרת אספקה
- **סשן חדש לכל משימה** (New Chat). הוטמנו **קבצים מיותרים** → שיקול דעת בבחירת רלוונטיים.
- **מסמך מפתח** בתרחיש עיכוב = **מדיניות הטיפול בעיכובים**. **אפקט השרשרת** = עיכוב אחד פוגע במשלוחים "בקנה".
- **אחריות**: המודל מאיץ, אבל האחריות והאימות על **הלומד**. **שקף המצגת** = רקע בלבד, **לא** לצרף לשיחה.
- **Incoterms** = תנאי סחר בינ"ל (אחריות ביטוח והובלה). הגשה: **בפרטי בצ'אט Zoom**.

## הרצאה 08 · Skills וחלון הקשר
- **קשב (Self-Attention)** = סיבוכיות **O(N²)** ריבועית → בסיס ל"ריקבון הקשר".
- **Lost in the Middle**: דיוק גבוה ב**קצוות** (Primacy + Recency), נמוך באמצע.
- **טוקניזציה**: מילה אנגלית ≈ **1.3** טוקן; עברית ≈ **3.25** (פי ~2.5, בגלל **BPE** שאומן על אנגלית). "מס הטוקנים" = **4** מיסים (פיננסי, חישובי, קיבולת, דיוק).
- **`/clear`** = סשן חדש (מאפס); **`/compact`** = דוחס תמצית באותו סשן.
- **4 מרכיבי הסוכן (Weng 2023)**: **LLM (מוח) · חלון הקשר (זיכרון) · כלים (ביצוע) · RAG (זיכרון ארוך-טווח)**.
- **GraphRAG**: גרף ידע היררכי + סיכומי קהילות → חוסך עד **90%** טוקנים בשאילתות גלובליות.
- **אוטומציה vs אוגמנטציה** → מומלץ **אוגמנטציה** (סוכן = "טייס משנה").
- **Progressive Disclosure** ב-SKILL.md = **3 רמות**: מטא-נתונים (תמיד) → הנחיות (בהפעלה) → משאבים (בביצוע).
- **חלון הקשר** גדל: **200K → מיליון**.

## הרצאה 09 · Git, GitHub ו-Gmail API
- **Client vs Token**: Client מעביר תקשורת ל-Google; **Token** מחזיק את ההרשאות.
- **לא מילת מפתח בודדת** לזיהוי (הופך ל-Rule-based) — הסוכן מזהה **כוונה לפי תוכן**.
- **4 תפקידי OAuth 2.0**: **Resource Owner · Client · Authorization Server · Resource Server**.
- **144 commits** = מקצועי מאוד; **commit אחד** = הכי לא מקצועי.
- **מחזור פיתוח (סדר!)**: **PRD → Plan → To-Do**. **README** = מה הפרויקט עושה + איך מריצים.
- **Git**: חוזרים לגרסה 1 לתיקון בזמן שגרסה 2 נשמרת. **Push** = מחשב→ענן; **Pull** = ענן→מחשב. פותח: **Linus Torvalds, 2005**.
- **Moat (חפיר)** = יתרון תחרותי בר-קיימא לפי **VRIN**: **V**aluable · **R**are · **I**nimitable · **N**on-substitutable.
- **אסטרטגיית החינם**: להתחיל בחינם, לצבור משתמשים לפני שמתחרים נכנסים. **דילול** = ויתור על % בעלות בכל סבב (יזם נשאר ~3–5%).

## הרצאה 10 · מולטימדיה
- **`.mp4`** = שם **מיכל (Container)** המכיל ערוצים. **"השמע מוביל"** = ערוץ השמע קובע קצב, השאר מיישרים.
- הפקת קליפ **"כמעט לא שותה טוקנים"** — המודל רק **מתזמר** בפקודות קצרות, לא מייצר מדיה כבדה.
- **CLI מקומי** ניגש לקבצים ומריץ **FFmpeg**; צ'אט דפדפן רק כותב פקודה. מקצב 4/4: תמונה בכל **ביט** = חיתוך מהיר פי 4.
- **מיומנות עוטפת FFmpeg** = תוכנית Python המתרגמת שפה טבעית לפקודת FFmpeg. **~90%** מהקטעים דורשים השתקת שמע.
- **היגיינת פרויקט** = ריכוז שמע+תמונות בתיקייה אחת שבה נפתח ה-CLI. **MCP** = תקן פתוח לחיבור סוכנים לכלים חיצוניים.
- **Agentic OS** = מעבר מ"ממוקד-יישומים" ל**"ממוקד-כוונה"** — סוכן יחיד מתזמר כלים לפי מטרה.

## הרצאה 11 · מצגות HTML בסוכן AI
- **מחסום הכלי**: מנהל לא-טכני צריך לשלוט בכלי מורכב → הסוכן **הופך** אותו: מתארים **כוונה בשפה טבעית**, הסוכן בונה.
- **Full-Stack** = **קצה-קדמי (Front-end)**: GUI, מה שרואים (דפדפן) + **קצה-אחורי (Back-end)**: שרת בענן, לוגיקה/אחסון/חישוב.
- **מבחן ניתוק הכבל**: נעצר → יש שרת (מלוא-מחסנית); ממשיך → **מקומי**.
  - *מלוא-מחסנית*: Gmail, WhatsApp, Yad2, Google Slides · *מקומי*: PowerPoint, מחשבון, שעון, **מצגת HTML**.
- **מצגת = אתר מקומי**. הדפדפן = **"נגן"** (URL = "שיר חדש"). **כל שקופית = דף HTML אחד**.
- **`index.html`** = דף הנחיתה שנפתח ראשון. שיתוף = **כל התיקייה** (index.html לבד ישבור — נשען על נכסים).
- **Pipeline (סדר!)**: צ'אט→בקשת **תסריט** → שמירה כ-**AI.txt** → סוכן **CLI** מייצר HTML → **לחיצה כפולה** על index.html.
  - מבקשים **תסריט ולא שקופיות** (שליטה לפני יצירה). עבודה **הדרגתית**. אותו מבנה כמו תרגיל השעון.
- **2 סוגי מצגות**: *מבוססת-אובייקטים* (צורות/גרפים, קרה, טכנית) · *מבוססת-תמונות* (עשירה, חגיגית).
- **שאיבת סגנון ב-JSON**: מפרידים סגנון מתוכן; JSON = **"גשר"** בין סוכן מנתח לסוכן בונה (כמו Suno למוזיקה).
- **3 שכבות הרשת**: **HTML** (מבנה/תוכן) · **CSS** (עיצוב) · **JavaScript** (התנהגות/ניווט). עיקרון = **הפרדת דאגות** (SoC).
  - `.js` ≠ Java (מתוקנן ב-ECMA-262). **BiDi** = כיווניות דו-כיוונית (Unicode UAX #9) — מונע היפוך עברית.
- **פס-קול**: FFmpeg ("מדביק תמונות עם קול") — עטיפת כלי בשפה טבעית (חיבור ל-L10).
- **תיאום רב-סוכני (Orchestration)**: סוכן **תסריטאי** ("מפיק הוליוודי") + סוכן **מקודד טכני** → שליט וניתן-לחזרה. מומחים > סוכן כללי.
- **מגבלות**: מהימנות קוד (**הזיות** → אדם בלולאה) · **נגישות** (צריך WCAG במפורש) · אבטחה/תלות בספקים. **חלופות**: reveal.js, Marp.

---

# ⚡ טבלאות שליפה מהירה

### סדרים (שאלות "הסדר הנכון")
| נושא | הסדר |
|------|------|
| Vibe Coding (L05) | הגדרת משימה → יצירת קוד → שמירה → הרצה |
| מחזור פיתוח (L09) | PRD → Plan → To-Do |
| Pipeline מצגת HTML (L11) | תסריט (צ'אט) → AI.txt → סוכן CLI מייצר HTML → לחיצה כפולה index.html |
| התקנת Codex (L03) | PowerShell → Node/npm → הרשאה → Codex → התחברות |

### רשימות "כמה/אילו רכיבים"
| מונח | הרכיבים |
|------|---------|
| 4 מרכיבי סוכן | LLM · חלון הקשר · כלים · RAG |
| RL | מצב · פעולה · תגמול · מדיניות |
| OAuth 2.0 | Resource Owner · Client · Auth Server · Resource Server |
| VRIN (Moat) | Valuable · Rare · Inimitable · Non-substitutable |
| 3 שכבות רשת | HTML (מבנה) · CSS (עיצוב) · JS (התנהגות) |
| מס הטוקנים | פיננסי · חישובי · קיבולת · דיוק (4) |

### מספרים לזכור
| נתון | ערך |
|------|-----|
| סיבוכיות קשב | O(N²) |
| טוקנים: אנגלית / עברית | ~1.3 / ~3.25 (פי ~2.5) |
| חלון הקשר: אז→היום | 200K → מיליון |
| GraphRAG חיסכון | עד 90% טוקנים |
| Noy & Zhang | −40% זמן, +18% איכות |
| טווח שליטה (ההשטחה) | 8.1 → 12.1 |
| TSMC נתח שבבים | 90%+ |

### הבחנות מבלבלות
- **רגרסיה** (רציף) ↔ **סיווג** (בדיד) ↔ **חריגות** (נדיר) ↔ **חיזוק** (תגמול).
- **`/clear`** (מאפס) ↔ **`/compact`** (דוחס). **Push** (→ענן) ↔ **Pull** (→מחשב).
- **Front-end** (רואים) ↔ **Back-end** (שרת). **`/exit`** (יוצא מ-Codex) ↔ **`exit`** (סוגר טרמינל).
- **מבוססת-אובייקטים** (טכנית/קרה) ↔ **מבוססת-תמונות** (חגיגית).

### עקרון-על של הקורס
**AI = כלי רב-עוצמה שמצריך "אדם בלולאה", אימות, וערך מוסף אנושי.** לעולם לא להעביר תוצר כמו-שהוא.
